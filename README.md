# Trace iOS Card Stitch v8

正式版本号：v8。

本次 v8 修订：
- 修复多角色字卡串用：角色只会从自己明确绑定的字卡组回复。
- 多角色环境中，旧版角色若仍处于“全选全部分组”，升级后会清空这类全选绑定一次，避免继续串角色；已经手动选成子集的绑定保留。
- 新建角色不再默认绑定全部现有字卡组，需要在角色详情中选择要用的分组。
- 修复拼字卡上限：开启拼字卡后每轮只发送 1 个拼接气泡，“一次最多拼”就是该气泡能使用的字卡绝对上限。
- 群聊同步使用严格的角色字卡绑定。

其余现有功能与本地数据结构保持兼容。
