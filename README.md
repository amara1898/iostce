# Trace v12

内部修订：v12-r10。

本次更新：

- 群聊输入区增加“表情与贴纸”，复用现有表情库；群成员自动回复表情时仍按各自绑定分组读取。
- 群聊消息增加成员头像；双击成员头像会触发“拍一拍”系统提示。
- “拍一拍”动作库移动到「设置 → 首选项 → 参数」，继续使用原有 `customPats` 数据。
- 群聊增加来电：一次可有一到多名成员同时呼叫，但只能选择一人接通；其余成员会在群聊中出现“用户正忙，请稍后再拨”。主动群聊来电采用低概率和 45 分钟冷却，并复用现有单人通话界面。
- 单聊聊天页顶部改为备注名优先，无备注时显示角色本名。
- v12 版本说明补入以上功能，并补记 r8 键盘保持、r9 日记/慢信自然排列与单次最多回复硬上限修复；「提示 → 最近更新」固定 5 行未改。

保留内容：

- r8：iOS 键盘打开时发送后保持；键盘已收起时发送不自动弹出。
- r9：日记与慢信回信按自然段排列；单聊和群聊每轮回复不超过“单次最多回复”。
- 未重新应用此前暂缓的 r6 stitch strict-max 行为。

部署：将本目录 7 个文件全部上传到 GitHub Pages 仓库根目录，确保 `index.html` 位于根目录。
